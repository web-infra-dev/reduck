{
  "version": "independent",
  "npmClient": "pnpm",
  "command": {
    "publish": {
      "ignoreChanges": [
      ],
      "message": "chore(release): publish"
    }
  },
  "packages": [
    "packages/*",
    "packages/plugins/*"
  ]
}
